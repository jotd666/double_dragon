- lockup at start, subcpu not doing the proper job maybe trace the subcpu in mame the second time
  it's activated (with registers)
  now that it's activated, it seems to loop, check if it reaches C27E or C0A7 and compare with execution log
  (maybe the cpu is setting 8000 at some point to unlock the subcpu, and we are screwed)

- hack OSD palette to make black appear (character heads)
- set DSW difficulty lives, etc.


- title fading in not supported

- mockup of title sprites (simpler? no subcpu used to transform coords?)
  
  optim for 6809 generator:
 	xxx_W_TO_REG	a0,d2                         	| [$5bd5: pulu   x,y]
	addq	#2,a0

=> xxx_W_TO_REG_POSTINC
	subq	#2,a0
 	xxx_W_TO_REG	a0,d2                         	| [$5bd5: pulu   x,y]
=>  xxx_W_TO_REG_PREDEC

monitor bank switch: wpset 3808:maincpu,1,W,,{printf "bank %d",(wpdata & 0xe0) >> 5;g}
force bank switch: fill 3808:maincpu,1,bankno<<5