beta/final:

- music for end
- set DSW difficulty lives, etc.
- level 2 conveyor belt color changes is color cycling. Maybe simulate it during mainloop hook if 3C is within some values
  palette entry changes are 117F 117E 117D

- continue (after highscore entry) game crashes like at end of game, but 
  clear credits to fix issue & get rolling demo after completing works
- level4 music has some hiccups: make a vid: send it!

- check throwing curly thug, first frame seems corrupt due to grouping
- when throwing curly thug, it seems that second player sprite is quickly visible
  (which is maybe not a bug)
- enable blitter chaining to speed things up: crash at start illegal blitlist debug this
- pre-mirror sprites per level
  - level 2: hooker, black guy with bat
  - level 3: curly hair guy
  - level 4: giants, punks
- possible to pre-mirror most character mask bitplanes?
- "CONTINUE" within game counts as "game not in play": detect and change
  would need some more RE as p1 could have "continue" while p2 is still playing

- ECS: limit to 1 clut, enable EHB extra plane of zeroes or ones if clut odd or even: saves memory and gives
  diversity. Then EHB can also be used for tiles. For red and blue, maybe palette can be ordered properly (powers of 2). Not sure with colors 16-31

- save option trashes the sprites!!!
- dump mem same key as kick
- in the end: perform fade out on tiles
- check values of max_scroll_x, min_scroll_x when loading level 2 and when reaching level 2 from start, check differences
- duplicate copperlist so color changes between levels are completely seamless: full screen redraw
  should be done in level1 interrupt with a counter (refresh drawn part only), using different draw tile functions
  => copperlist duplicated, now handle this
  
- 
- continue insert coin credit check stuff in highscore table and in intro cinematics

- profile real game/optimize (once code is frozen).
(6204, "('4', '412F', 'RTS')")
(4299, "(main, 'FC9A', 'PULS   A,PC')")
(4271, "(main, 'FC97', 'STA    $3808')")
(4136, "('4', '4562', 'BCS    $454A')")


- highscore load/save

- if a sound is only in one level and that level has 2 channel music, allocate it of course on channel 2



 
- adaptation of ptplayer to 255 instruments


- when resetting context, redraw all tiles as palette has changed but do not reset scroll, ever
  only reset x_scroll at level 1: to test, maybe only affects when loading/saving, in that case, it doesn't matter
- weak enemies: doesn't work with hooker and giant bosses


- try 16+16 color version (this is going to suck!)
  
  optim for 6809 generator: find same GET_DP_ADDRESS
	GET_DP_ADDRESS	0x56                    	| [$452e: ldx    $56]
	MOVE_W_TO_REG	a0,d2                         	| [...]
	add.w	#0x21,d2                         	| [$4530: leax   $21,x]
	GET_DP_ADDRESS	0x56                    	| [$4533: stx    $56]
	MOVE_W_FROM_REG	d2,a0                         	| [...]
 
 
- converter: maybe find a leas +/- stack size pattern, then scan the lines in-between to find
  stack references above the reserved limit (parameters)
- converter: add push/pull around add manual stack D5

monitor bank switch: wpset 3808:maincpu,1,W,,{printf "bank %d",(wpdata & 0xe0) >> 5;g}
force bank switch: fill 3808:maincpu,1,bankno<<5
weak enemies: bpset 631C,b@pc==$E7,{B=1;g}
log sounds: wpset 380E,1,W,wpdata!=$FF
record: bpset 89EC,,{bpclear;A=$10;g}

trace cpu with bank: first save bank number at each switch, then trace it

wpset 3808:maincpu,1,W,,{temp0=(wpdata & 0xe0) >> 5;g}
trace mame.tr,,noloop,{tracelog "bank=%d ",temp0}

winuae: check value written is FF
w 0 rA6+$A78 1 W VFF.B
w 1 rA6+$A85 1 W VFF.B
w 2 rA6+$A6D 1 W VFF.B

